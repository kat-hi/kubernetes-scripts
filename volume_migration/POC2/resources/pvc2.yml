apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: poctestpvc2
  namespace: testing
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2G
  storageClassName: ceph